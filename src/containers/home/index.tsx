import { FormattedMessage } from 'react-intl';
import PageContainer from '~/components/layout/PageContainer';

//* View
const Page = () => (
    <PageContainer>
        <FormattedMessage id="common.hotline" />
    </PageContainer>
);

export default Page;
